# Agent eval tests have moved to aver-experimental.
# See: https://github.com/njackson/aver-experimental
#
# This workflow is kept as a stub to avoid breaking references.
# Delete this file once aver-experimental has its own CI.

name: Eval

on:
  workflow_dispatch:

jobs:
  stub:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Agent eval tests have moved to aver-experimental"
